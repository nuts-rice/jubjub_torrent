use libp2p::{
    futures::StreamExt,
    identity,
    swarm::{Swarm, SwarmEvent},
    PeerId
    // mdns
};

use tokio::sync::oneshot;
use tokio::sync::mpsc;


async fn start_discovery() {unimplemented!()} 
